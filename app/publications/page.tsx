import { PageHeader } from '@/components/PageHeader'; import { PublicationCard } from '@/components/PublicationCard'; import { publications } from '@/lib/data';
export default function Publications(){return <><PageHeader eyebrow="Publications" title="Publications and manuscripts.">Bibliographic information is shown only where it is available. Journal, DOI, PDF, and BibTeX records will be added when assigned.</PageHeader><section className="container py-12"><p className="eyebrow">Manuscripts in progress</p><div className="mt-4">{publications.map(p=><PublicationCard key={p.title} publication={p}/>)}</div></section></>}
