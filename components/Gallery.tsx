'use client'; import Image from 'next/image'; import { useState } from 'react'; import { X } from 'lucide-react';
export function Gallery({items}:{items:readonly {name:string;code:string;text:string;images:readonly string[]}[]}){const [active,setActive]=useState<string|null>(null);return <><div className="grid gap-10 md:grid-cols-2">{items.map(item=><article key={item.code}><div className="grid grid-cols-2 gap-2">{item.images.map((src,i)=><button className={`relative overflow-hidden bg-[var(--wash)] ${i===0?'col-span-2 aspect-[16/9]':'aspect-square'}`} onClick={()=>setActive(src)} key={src} aria-label={`View ${item.name} image`}><Image src={src} alt={`${item.name} fieldwork`} fill className="object-cover transition hover:scale-105" sizes="(max-width: 768px) 100vw, 50vw"/></button>)}</div><div className="mt-4 flex gap-5"><span className="eyebrow">{item.code}</span><div><h2 className="font-serif text-2xl">{item.name}</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">{item.text}</p></div></div></article>)}</div>{active&&<div className="fixed inset-0 z-[60] grid place-items-center bg-black/85 p-5" role="dialog" aria-modal="true" aria-label="Image viewer"><button className="absolute right-5 top-5 text-white" onClick={()=>setActive(null)} aria-label="Close image"><X size={26}/></button><div className="relative h-[85vh] w-full max-w-6xl"><Image src={active} alt="Expanded fieldwork photograph" fill className="object-contain" sizes="100vw"/></div></div>}</>}
