'use client';
import Link from 'next/link'; import { Menu, X } from 'lucide-react'; import { useState } from 'react'; import { nav } from '@/lib/data'; import { ThemeToggle } from './ThemeToggle';
export function Navbar(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-[var(--line)] bg-[color:var(--paper)]/95 backdrop-blur"><div className="container flex h-16 items-center justify-between"><Link href="/" className="font-serif text-xl tracking-tight">Nimatullah<span className="text-[var(--pine)]">.</span></Link><nav className="hidden items-center gap-5 xl:flex" aria-label="Main navigation">{nav.map(x=><Link className="text-[.72rem] font-semibold uppercase tracking-[.09em] text-[var(--muted)] hover:text-[var(--ink)]" href={x.href} key={x.href}>{x.label}</Link>)}<ThemeToggle/></nav><div className="flex items-center gap-2 xl:hidden"><ThemeToggle/><button onClick={()=>setOpen(!open)} aria-label="Toggle navigation" className="grid h-9 w-9 place-items-center border border-[var(--line)]">{open?<X size={18}/>:<Menu size={18}/>}</button></div></div>{open&&<nav className="container grid border-t border-[var(--line)] py-3 xl:hidden" aria-label="Mobile navigation">{nav.map(x=><Link onClick={()=>setOpen(false)} className="border-b border-[var(--line)] py-3 text-sm" href={x.href} key={x.href}>{x.label}</Link>)}</nav>}</header>}
