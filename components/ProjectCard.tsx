import Image from 'next/image'; import Link from 'next/link'; import { ArrowUpRight } from 'lucide-react'; import type { Project } from '@/lib/data';
export function ProjectCard({project}:{project:Project}){return <article className="group grid border-t border-[var(--line)] py-7 md:grid-cols-[.95fr_1.3fr] md:gap-10">{project.figure?<div className="relative mb-5 aspect-[16/9] overflow-hidden bg-[var(--wash)] md:mb-0"><Image src={project.figure} alt={`${project.title} figure`} fill className="object-cover transition duration-500 group-hover:scale-[1.03]" sizes="(max-width: 768px) 100vw, 40vw"/></div>:<div className="mb-5 flex aspect-[16/9] flex-col justify-between bg-[var(--wash)] p-6 md:mb-0"><span className="eyebrow">Computational study</span><span className="font-serif text-3xl leading-tight">{project.technologies.slice(0,2).join(' · ')}</span></div>}<div><p className="eyebrow">{project.status}</p><h2 className="mt-3 font-serif text-3xl leading-tight">{project.title}</h2><p className="mt-3 text-[var(--muted)] leading-7">{project.short}</p><div className="mt-5 flex flex-wrap gap-2">{project.technologies.map(t=><span className="border border-[var(--line)] px-2 py-1 text-xs text-[var(--muted)]" key={t}>{t}</span>)}</div><Link href={`/projects/${project.slug}`} className="link-arrow mt-6 inline-flex items-center gap-1">Read project <ArrowUpRight size={15}/></Link></div></article>}
