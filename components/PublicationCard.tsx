import { ExternalLink, FileText } from 'lucide-react'; import type { Publication } from '@/lib/data';
export function PublicationCard({publication}:{publication:Publication}){return <article className="border-t border-[var(--line)] py-7"><div className="flex flex-wrap items-baseline justify-between gap-2"><h2 className="font-serif text-2xl">{publication.title}</h2><span className="text-xs font-semibold uppercase tracking-wider text-[var(--pine)]">{publication.status}</span></div><p className="mt-3 text-sm">{publication.authors} · <span className="italic">{publication.venue}</span> · {publication.year}</p><p className="mt-4 max-w-3xl text-sm leading-6 text-[var(--muted)]">{publication.abstract}</p><div className="mt-4 flex gap-4 text-sm text-[var(--pine)]">{publication.url&&<a className="inline-flex items-center gap-1" href={publication.url} target="_blank" rel="noreferrer"><ExternalLink size={14}/> {publication.linkLabel ?? 'Link'}</a>}<span className="inline-flex items-center gap-1 text-[var(--muted)]"><FileText size={14}/> {publication.status==='Published'?'PDF pending':'DOI / PDF pending'}</span></div></article>}
